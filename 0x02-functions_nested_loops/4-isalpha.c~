#include "main.h"
/**
*_isalpha - int c as input
*@c: checks input function
*Return: 0 or 1
*/
int _isalpha(int c)
{
if ( c >= 65 && c <= 122)
return (1);
if (c >= 97 && c <= 122)
return (1);
return (0);
}
